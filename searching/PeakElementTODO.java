package searching;

public class PeakElementTODO {
    public static void main(String[] args) {
        int[] arr = {5, 10, 20, 15, 7};

        System.out.println(findPeakElement(arr, 0, arr.length - 1));
    }

    private static int findPeakElement(int[] arr, int low, int high) {

//        if(low < high)
//            return -1;
//        int mid =  low + (high-low)/2;
//        if(mid == 0){
//            return mid;
//        }else if(arr[mid] > arr[mid-1] && (mid != arr.length-1 && arr[mid] < arr[mid+1]) ){
//            return mid;
//        }else if(){
//
//        }
        return 0;
    }
}
